# Scaffold Patterns

- Use official CLI tools whenever possible (`create-next-app`, `npm create vite@latest`, `uv init`, `django-admin startproject`).
- Avoid writing raw boilerplate files if a generator exists.
- Ensure to pass flags to avoid interactive prompts (e.g., `-y`, `--typescript`, `--tailwind`).
