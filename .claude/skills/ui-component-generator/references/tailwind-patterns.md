# Tailwind Patterns

- Use standard utility classes for layout (flex, grid, spacing).
- Create a consistent color palette using standard Tailwind colors.
- Use responsive modifiers (`sm:`, `md:`, `lg:`) for mobile-first design.
- Do not write custom CSS unless strictly necessary.
